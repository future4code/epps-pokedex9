import React from 'react'
import {HeaderStyle} from '../Styled/Styled'

function Header() {
  return (
    <HeaderStyle>
       <button> ir pokedex</button>
        <button> ir para pokedex</button>
    </HeaderStyle>
  )
}

export default Header
